/* eslint-disable @repo/no-null-render */
/** Langfuse Cloud v4 project chip. Hidden on Vết (pinned v4.33.0). */

export function V4MigrationProjectChip(_props: {
  project: { id: string; name: string };
  readiness: "action-needed" | "partner-managed";
}) {
  return null;
}
