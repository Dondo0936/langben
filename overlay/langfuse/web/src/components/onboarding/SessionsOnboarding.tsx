import React from "react";
import { SplashScreen } from "@/src/components/ui/splash-screen";

export function SessionsOnboarding() {
  return (
    <SplashScreen
      title="Chưa có phiên"
      description="Phiên gom các lượt cùng một hội thoại. Gửi thử trên Kênh (Lark / Google Chat) sẽ tạo phiên ngay."
    />
  );
}
